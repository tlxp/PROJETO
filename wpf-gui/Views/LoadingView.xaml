<UserControl x:Class="RatAnalyzer.Desktop.Views.LoadingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="8,0,8,12">
            <TextBlock Text="A preparar o ambiente de sandbox"
                       Style="{StaticResource TitleTextBlock}" />
            <TextBlock Text="A iniciar backend (porta 8000) e frontend (porta 8080); ao fechar a aplicação as portas são libertadas."
                       Style="{StaticResource SubtitleTextBlock}"
                       Margin="0,4,0,0" />
        </StackPanel>

        <!-- Main card -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*" />
                <ColumnDefinition Width="2.8*" />
            </Grid.ColumnDefinitions>

            <!-- Spinner + status -->
            <Border Grid.Column="0"
                    Margin="0,0,12,0"
                    CornerRadius="{StaticResource Radius.Large}"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrushBase}"
                    BorderThickness="1"
                    Effect="{StaticResource Shadow.Medium}">

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="32">
                    <!-- Loading Spinner -->
                    <Grid>
                        <Ellipse Width="110" Height="110"
                                 Fill="{StaticResource AccentSoftBrush}"
                                 Opacity="0.08" />
                        <Ellipse Width="90" Height="90" Stroke="{StaticResource AccentBrush}" StrokeThickness="8"
                             StrokeDashArray="6 2" RenderTransformOrigin="0.5,0.5">
                            <Ellipse.RenderTransform>
                                <RotateTransform x:Name="noFreeze" Angle="0" />
                            </Ellipse.RenderTransform>
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                             From="0" To="360" Duration="0:0:1.1" RepeatBehavior="Forever" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>
                    </Grid>

                    <TextBlock Text="A arrancar a sandbox em VM..."
                               Margin="0,22,0,0"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Tudo corre apenas em local; o dashboard abre automaticamente quando o ambiente estiver pronto."
                               Margin="0,4,0,0"
                               Style="{StaticResource MutedTextBlock}"
                               TextAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- Status Logs -->
            <Border Grid.Column="1"
                    Margin="12,0,0,0"
                    CornerRadius="{StaticResource Radius.Large}"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrushBase}"
                    BorderThickness="1"
                    Effect="{StaticResource Shadow.Medium}">

                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="Logs de arranque"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontSize="13"
                                   FontWeight="SemiBold" />
                        <TextBlock Text=" · "
                                   Foreground="{StaticResource TextMutedBrush}" />
                        <TextBlock Text="Backend e frontend (portas 8000 e 8080)"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11" />
                    </StackPanel>

                    <Border Grid.Row="1"
                            Margin="0,10,0,0"
                            CornerRadius="{StaticResource Radius.Medium}"
                            Background="{StaticResource PrimaryBackgroundBrush}"
                            BorderBrush="{StaticResource BorderBrushBase}"
                            BorderThickness="1">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Padding="10">
                            <ItemsControl ItemsSource="{Binding SystemLogs}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontFamily="Consolas"
                                                   FontSize="11"
                                                   Margin="0,1,0,1" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

