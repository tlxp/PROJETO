<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Ponto de entrada: escolher UM dos seguintes -->
    <Compile Remove="Program.cs" />
    <!-- Para sample simples (EICAR): descomentar a linha abaixo e comentar a de advanced_test -->
    <!-- <Compile Remove="advanced_test.cs" /> -->
    <!-- Para sample avançado (teste RAT/analyzer): descomentar a linha abaixo e comentar a de auraa -->
    <Compile Remove="auraa.cs" />
  </ItemGroup>

</Project>
